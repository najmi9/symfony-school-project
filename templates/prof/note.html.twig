
{% extends 'base.html.twig' %}

{% block title %}Anounce{% endblock %}

{% block body %}

<div class="container">
 
        {{ form_start(noteForm)}}
       
       {{ form_row(noteForm.note) }}
      
        {{ form_row(noteForm.student)}}

        <button class="btn btn-success btn-xl" type="submit">
        	<i class="fas fa-plus"></i>Note ! 
        </button>
        {{ form_end(noteForm)}}

</div>
{% endblock %}